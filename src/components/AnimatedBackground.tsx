/**
 * A deliberately quiet background. The previous floating icons made each visit
 * visually different and competed with the work itself.
 */
export const AnimatedBackground = () => <div className="paper-grain" aria-hidden="true" />;
